{
  "name": "thr34ds",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-platform threads app with local storage and atomic clock sync",
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
